# evals

- `golden/` — SPEC-01 catalog definitions + acceptance envelopes (frozen m01).
- `adversarial/` — lands with M04: over-scoped manifests, injection via asset
  metadata, envelope-gaming proposals (e.g. dropping required rungs to win on
  bits).
- `history/` — append-only SPEC-00 verdict files. Every number in the README
  resolves to a file here or in `milestones/`.
