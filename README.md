# mastercopy
Agentic per-title encoding on the real AWS Elemental stack — the agent proposes the ladder, the pipeline disposes. Same VMAF, fewer dollars, with a signed manifest and a human gate between the model and production. Built milestone-by-milestone with Claude Code — the repo history IS the demo.
